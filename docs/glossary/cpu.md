# CPU

> **Type**: noun | **Tier**: important | **Domains**: computing

(Central Processing Unit): Arithmetic Logic Unit (ALU) performs addition, subtraction, AND, OR, NOT, shifts. Control unit generates timing signals and decodes instructions. Register file: small fast storage (accumulator, index registers, program counter, instruction register, status flags).
