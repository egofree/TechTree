# Via rules

> **Type**: noun | **Tier**: important | **Domains**: vlsi-scaling

Vias (vertical connections between metal layers) have minimum size (e.g., 25 nm × 25 nm), minimum spacing, and enclosure rules (via must be fully surrounded by metal on both top and bottom layers by a minimum margin). Via arrays (multiple vias in parallel) improve reliability and reduce resistance.
