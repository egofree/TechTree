# Software stack

> **Type**: noun | **Tier**: important | **Domains**: vlsi-scaling

Drivers, compilers (HLS — high-level synthesis), programming models (CUDA/OpenCL equivalents), libraries (BLAS, FFT, neural network primitives).
