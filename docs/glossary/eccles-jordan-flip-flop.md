# Eccles-Jordan flip-flop

> **Type**: noun | **Tier**: important | **Domains**: computing

Two triodes cross-coupled (each plate drives the other's grid). Two stable states — one tube conducting, the other cut off. Trigger pulse toggles state. One bit of storage. Four tubes per flip-flop (two dual-triode envelopes).
