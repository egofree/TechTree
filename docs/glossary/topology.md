# Topology

> **Type**: noun | **Tier**: important | **Domains**: energy

Battery bank → inverter (DC → AC) → critical loads. Utility/generator → rectifier (AC → DC) → battery → inverter → loads (online UPS, continuous battery buffering). Or utility → load with battery/inverter on standby (offline UPS, switches to battery on utility failure — brief interruption).
