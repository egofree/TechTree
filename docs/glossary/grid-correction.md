# Grid correction

> **Type**: noun | **Tier**: important | **Domains**: photolithography

Measure overlay at multiple points on a calibration wafer. Compute the best-fit grid correction (translation, rotation, scaling) and apply to all subsequent wafers in the lot. Reduces systematic overlay errors (reticle scaling error, stage scaling error) to residual random errors.
