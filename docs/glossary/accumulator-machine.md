# Accumulator machine

> **Type**: material | **Tier**: important | **Domains**: computing

Single implicit register (accumulator) receives all ALU results. Instructions: `ADD address`, `SUB address`, `STORE address`, `LOAD address`. Simple to implement, few transistors. Most early architectures (EDVAC, EDSAC, PDP-8). 1-address instruction format.
