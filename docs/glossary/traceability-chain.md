# Traceability chain

> **Type**: noun | **Tier**: important | **Domains**: measurement

Working micrometer → calibrated against inspection gauge blocks → calibrated against master gauge blocks → calibrated against length standard. Each step adds uncertainty. Total uncertainty = root-sum-square of all contributing uncertainties.
