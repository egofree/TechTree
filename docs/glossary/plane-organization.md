# Plane organization

> **Type**: material | **Tier**: important | **Domains**: computing

A 256 × 256 core plane stores 64 Kbit. Planes are stacked to form words: a 16-bit memory at 64K addresses requires 16 planes, each with 65,536 cores, totaling 1,048,576 individually threaded cores.
