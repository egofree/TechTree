# NOR flash

> **Type**: noun | **Tier**: important | **Domains**: computing

Random access to any word (byte-level reads). Fast read (50-100 ns). Slow erase and write (block-level, seconds). Used for firmware storage and execute-in-place (XIP). Cell: one floating-gate transistor per bit.
