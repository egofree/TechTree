# Latching relay

> **Type**: noun | **Tier**: important | **Domains**: computing

Relay with second coil (or mechanical latch) that holds state after trigger pulse removed. Used for memory — the basis of sequential logic and early computer memory. One relay = one bit.
