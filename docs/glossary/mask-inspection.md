# Mask inspection

> **Type**: noun | **Tier**: important | **Domains**: photolithography

Die-to-die comparison (compare two identical pattern areas on same mask — any difference is a defect) or die-to-database (compare mask pattern against design data — detects both defects and dimensional errors). Inspection sensitivity: 0.25-1.0× minimum feature size.
