# Contact configuration

> **Type**: noun | **Tier**: important | **Domains**: computing

Double-pole, double-throw (2 form-C contacts per relay). Each pole has a common (C), normally-open (NO), and normally-closed (NC) terminal. Both poles switch simultaneously when the coil energizes. Provides two independent signal paths per relay.
