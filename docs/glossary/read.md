# Read

> **Type**: material | **Tier**: important | **Domains**: computing

Drive current through X and Y select wires at half-switching current each — only the core at the intersection receives full current, flipping its state. Sense wire detects the induced voltage pulse (or absence thereof). Destructive read — must rewrite after reading.
