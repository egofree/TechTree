# Instruction cycle

> **Type**: material | **Tier**: important | **Domains**: computing

Fetch instruction from memory address in program counter → increment program counter → decode instruction → fetch operands → execute in ALU → store result → repeat. Clock rate: 100 kHz (early tube machines) to 1-10 MHz (transistor machines). One instruction typically takes 1-20 clock cycles.
