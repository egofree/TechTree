# Simultaneous carry problem

> **Type**: noun | **Tier**: important | **Domains**: computing

Multi-digit carries (e.g., 999 + 1) must propagate through all digits. The Pascaline uses a staggered pawl arrangement where each carry lever is slightly offset, so carries ripple leftward in sequence rather than all at once. This prevents mechanical binding from simultaneous forces.
