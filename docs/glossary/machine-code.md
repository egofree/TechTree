# Machine code

> **Type**: noun | **Tier**: important | **Domains**: computing

Binary instruction patterns loaded directly into memory. Each instruction = opcode + operand(s). Example: 0010 0001 0101 = "add contents of address 21 to accumulator." Error-prone, no readability. Earliest programs entered via front-panel switches (toggle binary into memory locations).
