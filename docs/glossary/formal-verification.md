# Formal verification

> **Type**: noun | **Tier**: important | **Domains**: vlsi-scaling

Mathematical proof that two representations are equivalent. Equivalence checking proves synthesis output matches RTL. Property checking proves specific assertions hold for all possible input sequences (e.g., "FIFO never overflows"). Eliminates the need to simulate every possible input combination — impossible for large designs.
