# Storage hierarchy (latency range)

> **Type**: noun | **Tier**: supporting | **Domains**: computing

The layered memory architecture in electronic computing systems, ranging from fast registers and cache (nanoseconds) through RAM (tens of nanoseconds) to disk storage (milliseconds), trading speed for capacity.
