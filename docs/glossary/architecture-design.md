# Architecture design

> **Type**: noun | **Tier**: important | **Domains**: vlsi-scaling

Parallel processing units, memory hierarchy (register file → L1 cache → L2 cache → DRAM), I/O interfaces. Hardware description language (HDL) specifies behavior.
