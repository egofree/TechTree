# Doping type test

> **Type**: noun | **Tier**: important | **Domains**: silicon

Hot probe (heated probe at one contact point, cold at another) generates a thermoelectric voltage whose polarity indicates n-type (negative voltage at hot probe) or p-type (positive voltage at hot probe).
