# Diode-transistor logic (DTL)

> **Type**: noun | **Tier**: supporting | **Domains**: computing

An early transistor logic family where input diodes perform the AND function and a transistor inverts it to create NAND/NOR gates. Replaced resistor-transistor logic (RTL) with better noise margins and fan-out; superseded by transistor-transistor logic (TTL).
